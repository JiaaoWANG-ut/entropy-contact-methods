# OVITO helpers

Scripts to extract frames from GPUMD `dump.xyz`, render with Tachyon (+ AO),
and stitch temperature–time summary grids.

Edit paths in `extract_frames.py` / `render_*.py` for your local dump and
output directories before submitting jobs.
